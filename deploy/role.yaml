apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  name: certman-operator
rules:
# secrets access is cluster-scoped, not restricted to Hive-managed cluster
# namespaces: the controller reads/writes secrets via mgr.GetClient(), whose
# cache requires list+watch to sync before any Get() succeeds (a ClusterRole
# with only get/create/update breaks it - see rosaeng-61300-rbac-verification.md).
# list/watch/create/update/patch cannot be scoped to "Hive-managed namespaces"
# with plain RBAC: those namespaces are created dynamically per-cluster, so
# there's no static namespace list or resourceName to restrict against.
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
# configmaps is restricted to the single named aws-account-operator ConfigMap
# (pkg/clients/aws/route53.go reads aaov1alpha1.DefaultConfigMap from the
# aws-account-operator namespace, not this operator's own namespace) since
# resourceNames is the only least-privilege lever available for a ConfigMap
# that lives in a namespace this operator doesn't own.
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  resourceNames:
  - aws-account-operator-configmap
- apiGroups:
  - ""
  resources:
  - namespaces
  verbs:
  - get
- apiGroups:
  - monitoring.coreos.com
  resources:
  - servicemonitors
  verbs:
  - get
  - create
- apiGroups:
  - certman.managed.openshift.io
  resources:
  - '*'
  - certificaterequests
  verbs:
  - '*'
- apiGroups:
  - hive.openshift.io
  attributeRestrictions: null
  resources:
  - clusterdeployments
  - clusterdeployments/finalizers
  - clusterdeployments/status
  verbs:
  - get
  - list
  - watch
  - update
  - patch
- apiGroups:
  - route.openshift.io
  resources:
  - routes
  verbs:
  - '*'
- apiGroups:
  - aws.managed.openshift.io
  resources:
  - accountclaims
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - hive.openshift.io
  resources:
  - dnszones
  verbs:
  - get
  - list
  - watch