inheritance: true
reviews:
  path_filters:
    # Exclude build artifacts and dependencies
    - "!build/**"
    - "!.venv/**"

    # Exclude test fixtures
    - "!**/testdata/**"
    - "!**/.test-fixtures/**"
