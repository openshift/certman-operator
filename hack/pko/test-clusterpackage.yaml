apiVersion: package-operator.run/v1alpha1
kind: ClusterPackage
metadata:
  name: certman-operator
  annotations:
    package-operator.run/collision-protection: IfNoController
spec:
  image: quay.io/redhat-services-prod/certman-operator-pko:${GIT_HASH}
  config:
    image: quay.io/redhat-services-prod/openshift/certman-operator:${GIT_HASH}
